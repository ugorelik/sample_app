<h1>All users</h1>

<%= will_paginate %>

<ul class="users">
	<%= render @users%>
</ul>

<%= will_paginate %>

<!-- Notes: if we do render user, rails automatically looks for a _user.html.erb
partial-->